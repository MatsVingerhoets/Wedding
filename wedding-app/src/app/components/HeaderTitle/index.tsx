import { Grid, Typography } from "@mui/material"

const HeaderTitle = (): JSX.Element => {
  return (
    <Grid container justifyContent="center">
      <Typography variant="h1">Jill & Mats</Typography>
    </Grid>
  )
}
export default HeaderTitle
